{
  "$schema": "../module.schema.json",
  "id": "redis",
  "name": "Redis",
  "description": "Shared ioredis client + BullMQ connection helpers + Pub/Sub-safe createRedisConnection().",
  "version": "1.0.0",
  "icon": "fas fa-database",
  "tags": ["infrastructure", "core", "cache", "queue"],
  "priority": 2,
  "dependencies": { "requires": ["env"] },
  "exports": {
    "redis": "redis.service.ts",
    "createRedisConnection": "redis.service.ts",
    "getBullMQConnection": "redis.bullmq.ts",
    "createQueue": "redis.bullmq.ts"
  }
}
