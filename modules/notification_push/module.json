{
  "$schema": "../module.schema.json",
  "id": "notification_push",
  "name": "Web Push Notifications",
  "description": "Web Push (VAPID) — subscription storage + send.",
  "version": "1.0.0",
  "icon": "fas fa-bell",
  "tags": [
    "notifications",
    "push"
  ],
  "priority": 30,
  "dependencies": {
    "requires": [
      "db",
      "env"
    ]
  },
  "author": "Kuray Karaaslan",
  "homepage": "https://github.com/kuraykaraaslan/next-boilerplate/tree/main/modules/notification_push",
  "license": "CC-BY-NC-ND-4.0",
  "extensionPoints": [
    {
      "id": "push:provider",
      "kind": "provider",
      "description": "A push-notification delivery backend (Web Push/VAPID, FCM, Expo, …). Sandboxed community plugins (the @push/* family) contribute their send op here; the Web Push payload encryption + VAPID key stay host-side via the webpush capability."
    }
  ]
}
