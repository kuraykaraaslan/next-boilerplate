{
  "$schema": "../module.schema.json",
  "id": "tenant_setting",
  "name": "Tenant Setting",
  "description": "Per-tenant key-value settings store (overrides system defaults).",
  "version": "1.0.0",
  "icon": "fas fa-sliders",
  "tags": ["tenant", "configuration"],
  "priority": 22,
  "dependencies": { "requires": ["db", "tenant"] }
}
