{
  "$schema": "../module.schema.json",
  "id": "tenant_branding",
  "name": "Tenant Branding",
  "description": "White-label branding: logo, favicon, colors, font, custom CSS — read by the proxy/shell to render per-tenant chrome.",
  "version": "1.0.0",
  "icon": "fas fa-palette",
  "tags": ["tenant", "branding", "white-label"],
  "priority": 23,
  "dependencies": { "requires": ["db", "tenant_setting", "storage"] },
  "settingKeys": ["BRANDING_KEYS"]
}
