export enum UserSecurityMessages {
  SECURITY_RECORD_ALREADY_EXISTS = 'Security record already exists for this user',
  SECURITY_RECORD_NOT_FOUND = 'Security record not found',
}

export default UserSecurityMessages;
