{
  "$schema": "../module.schema.json",
  "id": "mail_smtp",
  "name": "Mail — SMTP",
  "description": "SMTP email backend for the notification_mail module.",
  "version": "1.0.0",
  "icon": "fas fa-envelope",
  "tags": ["notifications", "mail", "provider"],
  "priority": 31,
  "enabled": true,
  "author": "Kuray Karaaslan",
  "license": "CC-BY-NC-ND-4.0",
  "dependencies": { "requires": ["notification_mail", "env", "setting"] },
  "extensions": [
    {
      "point": "mail:provider",
      "key": "smtp",
      "export": "mail_smtp/server/mail.extension",
      "metadata": { "label": "SMTP" }
    }
  ]
}
