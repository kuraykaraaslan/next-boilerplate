{
  "$schema": "../module.schema.json",
  "id": "user",
  "name": "User",
  "description": "Core user CRUD: create, find, update, deactivate. Foundation for every auth and tenancy flow.",
  "version": "1.0.0",
  "icon": "fas fa-user",
  "tags": ["identity", "core"],
  "priority": 10,
  "dependencies": { "requires": ["db", "env", "logger", "common"] }
}
