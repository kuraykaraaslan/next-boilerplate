import { LoadingScreen } from '@kuraykaraaslan/common/ui/loading-screen.component';

// Root loading boundary — same shared design as the tenant loading + error screens.
export default function RootLoading() {
  return <LoadingScreen />;
}
